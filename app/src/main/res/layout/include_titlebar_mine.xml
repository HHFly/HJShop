<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/titlebar_height"
    android:background="@color/color_main_blue">


    <TextView
        android:id="@+id/titlebar_tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/home_我的"
        android:textColor="#ffffff"
        android:textSize="@dimen/titlebar_title_textsize" />

    <TextView
        android:id="@+id/titlebar_tv_return"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:paddingLeft="50px"
        android:paddingRight="50px"
        android:text="@string/titlebar_退出"
        android:textColor="#ffffff"
        android:textSize="@dimen/titlebar_title_textsize_sub" />

</RelativeLayout>